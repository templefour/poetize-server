[build]
builder = "nixpacks"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar poetry-web/target/poetize-server.jar --spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver"

